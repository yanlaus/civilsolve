# Only install package versions published at least 3 days ago - a buffer
# against a hijacked release being pulled in before it is caught. npm 11.x
# (min-release-age, in days); this replaced bunfig.toml when bun.lock went.
min-release-age=3
